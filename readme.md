# Spring AOP: annotation-based aspects

**problem**: the advised repository interface methods are not found by IDEA 
see `SampleAspect.aroundRepoMethodExecution()` and `NewAspect.beforeTargetRepo()` methods.

